services:
  _defaults:
    autowire: true
    autoconfigure: true

  RobinDort\PslzmeLinks\Service\DatabaseConnection:
    public: true

  RobinDort\PslzmeLinks\Controller\BackendRequestHandlerController:
    public: true

  RobinDort\PslzmeLinks\EventListener\InstallListener:
    arguments:
      $kernel: "@kernel"
    tags:
      - { name: contao.hook, hook: initializeSystem, method: copyTemplates }
